public enum ResourceType
{
    DataAsset,
    Tile,
    Character,
}
public enum CharacterType
{
    Player,
    Enemy,
}

public enum SpawnType
{
    Position,
    PrevWave,
}

public enum TableType
{
    Stage,
    Wave,
    Character,
    Status,
    Length
}